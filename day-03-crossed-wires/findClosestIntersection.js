/**
 * calculates postion points based on wire directions
 * @param {array} wire 
 */
const calcPath = (wire) => {
   let path = [];

   //loop wire directions
   //calc path

   return path;
};

const findClosestIntersection = (blueWire, redWire) => {

};

module.exports = findClosestIntersection;